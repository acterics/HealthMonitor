machine:
  environment:
    ANDROID_HOME: $ANDROID_HOME
    GRADLE_OPTS: '-Dorg.gradle.jvmargs="-Xmx1536M -XX:MaxPermSize=512M -XX:ReservedCodeCacheSize=512M"'

  java:
      version: oraclejdk8

dependencies:
  cache_directories:
    - ~/.android
    - ~/android

general:
  artifacts:
    - /home/ubuntu/HealthMonitor/app/build/outputs/apk/
deployment:
  beta:
    branch: master
    commands:
      - ./build.sh